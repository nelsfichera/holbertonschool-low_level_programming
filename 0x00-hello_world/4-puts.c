#include <stdio.h>
#include <string.h>
/**
* main - puts a line of text on stdout
* Return: 0
*/
int main(void)
{
	char str[51];
	strcpy(str, ""\"Programming is like a building a multilingual puzzle");
	puts(str);
	return (0);
}
