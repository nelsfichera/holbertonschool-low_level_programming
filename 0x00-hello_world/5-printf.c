#include <stdio.h>
/**
* main - it's a gd printf....
* Return: what the function returns
*/

int main(void)
{
	printf("%s\n", "with proper grammar, but the outcome is a piece of art,");

	return (0);
}
